#include <iostream>

int main(){
  //The first code in the for loop is the starting condition, the second is the ending condition immediately the condition is broken the for loop will end, and the last is the increment condition
  for (int i = 10; i >= 0; i--) {
    std::cout << i << '\n';
  }
    std::cout << "Happy New Year!!\n";


  return 0;
}